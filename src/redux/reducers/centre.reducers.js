/*import { GET_CENTER_BY_ID } from "../actions/centre.actions";

const initialState = {}

export default function CentreReducerById(state=initialState, action)
{
    switch (action.type) {
        case GET_CENTER_BY_ID:
            return action.payload;   
        default:
            return state;
    }
}*/